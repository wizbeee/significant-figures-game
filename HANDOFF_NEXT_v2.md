# 인수인계 — 유효숫자 마스터 개선 (2026-09-04 갱신)

> **새 세션은 이 문서를 먼저 끝까지 읽으세요.** (이 파일은 개선 작업 전용 인계서입니다.
> 게임 자체의 운영 가이드는 저장소 루트의 `HANDOFF.md`를 참고.)

---

## ⚠ 시작할 때 반드시 먼저 할 것

```bash
cd <저장소 경로>
git fetch origin
git checkout feat/learning-ux-v2      # 작업 브랜치
git log --oneline origin/main..HEAD   # 아래 커밋 3개가 보여야 함
npm test                              # 35/35 통과해야 정상
```

- **이 프로젝트는 여러 PC에서 작업됩니다.** 실제 운영 클론은 **학교 노트북**
  (`C:\Users\danie\Desktop\클로드 코드 관련\sigfig-game`)에 있고, 개선 작업은
  집 PC(`C:\Users\danie\Desktop\Claude Code\significant-figures-game`)에서 했습니다.
  **작업 시작 전 `git fetch` 를 생략하면 낡은 코드 위에서 작업하게 됩니다** (한 번 겪음, 아래 이력 참고).
- 운영 환경: 학교 노트북 1대 로컬 실행 + 학교 와이파이 IP 접속. 시즌제(점심 12:30~13:30,
  저녁 18:30~19:30). **Render 등 클라우드 배포는 포기함**(무료 플랜 디스크 미지원 확인).

---

## 지금 상태 한눈에

**작업 브랜치**: `feat/learning-ux-v2` (`origin/main`에서 분기, **GitHub 푸시 완료**)

| 커밋 | 내용 | 상태 |
|---|---|---|
| `a6037b9` | style: 기본 테마를 라이트로 전환 | ✅ 완료·검증(브라우저) |
| `613d4b4` | feat(review): 종료 화면에서 틀린 문제 해설 재열람 | ✅ 완료·검증(실서버) |
| `76b391f` | feat(auth): 명단 등록 학생만 입장 토글 | ✅ 완료·검증(실서버) |

- `npm test` **35/35 통과** (기존 28 + 신규 `test/normName.test.js` 7).
- 작업 트리 깨끗. 원격 `main` 브랜치는 **건드리지 않음**(PR/머지는 발주자 결정 대기).
- **원격 `main`에 병합할지는 아직 미정.** 학교 노트북에서 운영 중이므로, 머지·배포는
  발주자 확인 후 진행.

**작업 방식** (발주자 지정): 계획은 Fable 5.1, 구현은 Opus 5 하위 에이전트, 검증은 메인
세션이 실서버·브라우저로 독립 확인. Opus 5가 서버 과부하로 실패하면 잠시 대기하거나 Opus 4.8로 대체.

---

## 다음에 할 일 (권장 순서)

### ✅ 완료 (2026-09-04) — 학습 자료 사실 오류 A·B 수정

**대상**: `C:\Users\danie\Desktop\수업_활동자료\인터렉티브 학습 자료(유효숫자와 불확정도-베타버전).html`
- 백업: 같은 폴더에 `..._backup_원본.html` (수정 전 원본, 63,809 B). 되돌릴 땐 이 파일로 덮어쓰기.
- 변경 라인 41줄(추가/삭제 합). **9섹션 구조·팔레트·모바일·banker's rounding 섹션은 손대지 않음.**
- git 관리 대상이 아닌 파일이라 커밋 없음. 파일 자체가 최신 상태.

**🔴 오류 A — 저울 유효숫자 불일치 → 해결**
- 분석저울 버튼 `data-precision` `0.001` → `0.0001`, `<small>` 라벨도 `0.0001 g 단위`로.
- `updateBalance()`의 `decimals` 삼항을 `... : (precision >= 0.001 ? 3 : 4)`로 확장.
- 해설 문구 `0.001 g 자리` → `0.0001 g 자리`. 추가로 **0.001용 분기(5개)를 새로 넣어**
  버튼 구성이 바뀌어도 표시값과 해설이 어긋나지 않게 방어.
- **브라우저 실검증**: 4개 버튼 전부 클릭해 표시값·spec·유효숫자·해설 일치 확인.
  `1 g→25`(2개) / `0.1→24.7`(3개) / `0.01→24.68`(4개) / `0.0001→24.6783`(6개, 2·4·6·7·8·3) ✓

**🟠 오류 B — 동전 밀도 데이터 → 해결 (동전 2개 안 채택)**
- 발주자 메모가 "부피 ~1 mL / 상대불확정도 ~10%는 7번 섹션과 이어지는 교육적 이득"이라
  적어둔 쪽을 채택. **50원 동전 2개**로 설정.
- 새 데이터: `m = 8.3204 ± 0.0001 g`(분석저울), `V₁ = 11.55`(그대로), `V₂ = 12.51 ± 0.05 mL`
- 5단계 검산 결과(node로 재계산 후 화면 문구와 대조):

  | 단계 | 값 |
  |---|---|
  | 1 부피 | 12.51 − 11.55 = 0.96 mL, ±0.1 → **V = 1.0 ± 0.1 mL** |
  | 2 상대불확정도 | 질량 0.0001/8.3204 = 0.0000120 / 부피 0.1/0.96 = 0.10417 |
  | 3 합 | 0.10418 (= 10.42 %) |
  | 4 밀도 | 8.3204/0.96 = **8.667 g/mL** |
  | 5 절대불확정도 | 8.667 × 0.10418 = 0.9029 |
  | 최종 | **d = 8.7 ± 0.9 g/mL** (참값 8.7 대비 −0.38 %) |

- 마무리 💡 문구를 다시 씀: 이제 값이 맞으니 "왜 틀렸나"가 아니라 **"왜 불확정도가 10%인가"**를
  설명 — 상대불확정도의 99.99%가 부피에서 왔고, 7번 섹션 측정 기구 고르기로 연결.
- 헤더·데이터 제목도 "동전 2개"로 맞춤. `nextDensityStep()`은 UI 노출만 하고 수치는 전부
  마크업 하드코딩이라 **JS는 수정 불필요**했음.
- **브라우저 실검증**: 5단계 순차 노출, 최종 `d = 8.7 ± 0.9 g/mL` 표시, 완료 후 버튼 비활성,
  `처음부터` 리셋 정상 복귀, 콘솔 에러 0건 ✓
- 참고로 계산해 둔 대안(더 작은 불확정도를 원하면): **동전 25개** `m = 104.00 g`,
  `V₂ = 23.50` → `d = 8.70 ± 0.07 g/mL`. 지금 자료를 그대로 쓸 거면 무시해도 됨.

---

### ✅ 완료 (2026-09-07) — 학습 자료 GitHub Pages 공개 + 단위 변환 블록 추가

**공개 주소: https://wizbeee.github.io/significant-figures-game/** (로그인 불필요, 폰·PC 확인)
- Pages 소스 = `main` 브랜치 `/docs`. `docs/index.html`이 학습 자료 사본, `.nojekyll` 동봉.
- **자료를 고쳤으면 `docs/index.html`에 복사 후 main에 push해야 반영됩니다.** 데스크톱 원본
  (`수업_활동자료/...html`)이 여전히 정본이고, docs는 배포 사본입니다. 둘이 어긋나지 않게 주의.
- ⚠ 로컬 `main`은 아직 채택 안 한 `5195af1`에 머물러 origin과 갈라져 있음. Pages 작업은
  `origin/main`에서 새 브랜치를 따서 `git push origin HEAD:main`으로 올렸음. **로컬 main을
  그냥 push하면 안 됨.**

**단위 변환 블록** (섹션 4 "어떻게 적어야 할까?" 안, 퀴즈 위에 추가 — 9섹션 구조 유지)
- 기구가 불확정도를 정함: 자(아날로그) = 최소 눈금의 절반 / 디지털 = 최소 단위 1
- 자: `3.05 ± 0.05 cm` --(÷100)--> `0.0305 ± 0.0005 m`
- 마이크로비트: `593 ± 1 mg` --(×9.80÷1000)--> `5.81 ± 0.01 m/s²`
  - **mg는 밀리그램이 아니라 밀리 g**(중력가속도의 1/1000). 자료 안에도 명시해 둠.
- 평균을 내도 기기 한계는 그대로라 ±1이 안 줄어든다는 점, g를 9.80으로 쓰는 이유(9.8이면
  유효숫자 2개로 깎임)도 함께 안내.
- **구현 주의**: 기존 `.balance-btn`에 섹션 1 클릭 핸들러가 전역으로 걸려 있어서, 같은 클래스를
  재사용하면 `updateBalance(NaN)`이 돼 저울이 깨짐. 그래서 독립 클래스 `.uc-btn` 사용.
  `.calc-line`의 `word-break: break-all`도 숫자를 중간에 자르길래 이 블록에만 스코프해서 완화.
- **발주자 사양 중 일부러 뺀 것**: "불확정도 전파는 안 합니다" 문장. 그건 통합과학 활동지
  기준이고, 이 자료의 9번 섹션은 오차 전파를 가르치므로 그대로 넣으면 모순됨. 단위 변환은
  값·흔들림에 같은 수를 곱하는 것이라 전파와 별개라는 점만 반영.

---

### ✅ 완료 (2026-09-07) — 지적 사항 13건 일괄 수정

**학습 자료** (배포 반영 완료, `docs/index.html` 동기화됨)

| # | 내용 |
|---|---|
| 1 | **완료 화면이 DOM을 파괴하던 문제 해결.** `main-container`를 `innerHTML`로 갈아끼우던 것을 별도 `#complete-container` 토글로 변경. `다시 보러 가기`로 복귀 가능 |
| 2 | **`localStorage` 진행 저장 신설.** 섹션 위치 + 섹션별 점수 저장, 재방문 시 "지난번에 N번까지 봤어요" 배너. **저장소를 못 쓰는 환경에서도 정상 동작**(전부 try/catch) |
| 3 | **점수 집계를 4·5·7번까지 확대**(기존 2·6번만). 완료 화면에 섹션별 + 합계 점수판 |
| 4 | **문제 셔플**(Fisher-Yates) + **문항 17 → 30개**로 보강. 선택지 순서는 해설·기구 배열 의미 때문에 유지 |
| 5 | 손글씨 폰트 폴백을 `serif` → sans 계열로 변경 |

**게임** (`feat/learning-ux-v2`에 커밋, **main 미머지 — 배포는 발주자 결정**)

| # | 내용 |
|---|---|
| 6 | **gm3 정답 노출 해결.** `viewQuestion()`에 gm3용 `hide` 분기 추가 — 문제 단계엔 `{type, val, unit}`만 전송. `sf`(정답)는 항상 제거, `dv`는 디지털 저울(`scale`)일 때만(화면에 실제로 찍히므로). **잔여 노출**: `val`은 캔버스로 기구를 그리는 데 필요해서 못 뺌 → 완전 차단은 서버 렌더링 등 재설계 필요 |
| 7 | **`/api/state` 압축 적용.** `sendJSONCompressed()` 신설(기존 `maybeCompress` 재사용). **실측 80% 절감**(25명 방 기준 2974 → 601 bytes, brotli). 수업 1회 egress ~1GB → ~200MB |
| 8 | `/api/teacher/students`에 `stripSecretish()` 가드 추가. **원래 지적은 과장이었음** — 학생 레코드엔 토큰·비밀번호가 없고(토큰은 `persistedTokens` 별도) 교사 인증 뒤라 실제 유출은 없었음. 허용목록은 교사 화면이 뱃지·연속일수 등 여러 필드를 써서 하나만 빠뜨려도 조용히 깨지므로 **차단목록으로 처리** |
| 9 | `teacher.html` 죽은 코드 3개 함수(`loadAutoApprove`/`applyAutoApproveUI`/`toggleAutoApprove`) + 주석 처리된 호출부 제거 (23줄) |
| 10 | **README 오정보 정정.** "무료 플랜 … 영구 디스크 1GB"는 사실이 아님 — `plan: free` + `disk:` 조합이 거부되는 게 Render 배포가 막힌 원인. 무료 24시간 대안(영구 디스크 있는 무료 VM)도 함께 기재 |

**저장소·운영**

| # | 내용 |
|---|---|
| 11 | **로컬 `main` 정리.** 미채택 커밋 `5195af1`은 `archive/growth-system-unadopted` 브랜치로 보존한 뒤, `main`을 `origin/main`에 맞춤. 이제 실수로 push해도 안전 |
| 12 | **배포 사본 동기화 자동화.** `npm run sync-docs` (`scripts/sync-learning-material.sh`) — 원본을 `docs/index.html`로 복사하고, 이미 같으면 아무것도 안 함(줄바꿈 차이는 무시). PC별 경로 후보를 훑고 인자·`LEARNING_MATERIAL_PATH`로도 지정 가능 |
| 13 | 이 인계서 커밋 |

**검증**: `npm test` 35/35, 수정 파일 전부 JS 구문 검사 통과, 별도 `DATA_DIR`로 서버를 띄워
gm3 응답과 압축률을 실제 HTTP로 확인, 학습 자료는 라이브 사이트에서 저장·복원·완료 화면 복귀까지 확인.

---

### 🚧 진행 중 (2026-09-07) — 게임 24시간 공개 운영

**결정**: Oracle Cloud Always Free VM. (GCP 무료는 월 egress 1GB라 탈락 — 압축 후에도
수업 1회 ~200MB. Render 무료는 디스크 불가. 노트북 24시간은 발주자가 불가하다고 함.)

**공개 전에 발견해 고친 것 — 이게 제일 중요**
- `x-forwarded-for`를 무조건 신뢰하고 있었음. 프록시 없이 인터넷에 직접 노출하면
  요청마다 가짜 XFF를 넣어 **교사 로그인 레이트리밋(5분 10회)을 무제한 우회** 가능.
  게다가 **교사 비밀번호 기본값이 `3000`(4자리)** 이라 조합이 1만 개뿐 → 공개 즉시 위험.
  → `TRUST_PROXY` 환경변수로 게이팅. 실측: 위조 IP 20회 중 10회 차단(종전 0회).
- 기동 시 공개 운영 설정 안내 추가. **교내 LAN이면 현 설정이 맞다**는 점도 같이 적어
  잘못된 조치를 유도하지 않게 함.

**만들어 둔 것**: `deploy/setup-server.sh` (Ubuntu 1회 설치 — Node·전용계정·systemd
자동재시작·Caddy HTTPS 자동발급·방화벽), `deploy/README.md` (Oracle 절차·운영·백업·한계).
- Node는 `127.0.0.1`에만 바인딩, 항상 Caddy 경유. 데이터는 `/var/lib/sigfig` 권한 750.
- 앱의 모든 쓰기가 `DATA_DIR` 안에만 일어나는 것을 확인하고 `ProtectSystem=strict` +
  `ReadWritePaths` 적용.
- ⚠ **스크립트는 Ubuntu에서 실행 검증하지 못함**(이 PC에 Docker·WSL 없음). `bash -n`
  구문 검사만 통과. 배포 환경변수 조합으로 앱 기동·바인딩·CORS·교사 로그인은 실제로 확인함.

**✅ 발주자 결정 반영 (2026-09-07)**
- **`feat/learning-ux-v2` → `main` 병합 완료** (`be5d78f`, 충돌 없음, 푸시됨).
  병합본으로 재검증: npm test 35/35, 위조 IP 20회 중 10회 차단, gm3 정답 제거 확인.
  ⚠ **학교 노트북은 아직 낡은 코드**임 — 서버 정상 종료(Ctrl+C) 후 `git pull` 필요.
- **학생 인증**: 일단 **명단 제한만으로 운영**하기로 함(연습·복습 용도 전제).
  이 서버 기록을 성적에 직접 반영하게 되면 학생별 비밀번호가 선행 과제.

**🔴 남은 것 — 발주자만 가능**
1. **Oracle Cloud 계정 생성 + VM 만들기** — 본인 확인·카드 등록이 필요해 대신 못 함.
2. VCN 보안 목록에 TCP 80·443 수신 규칙 추가 (이걸 빠뜨리는 사고가 가장 흔함).
3. 도메인(HTTPS용). 없으면 DuckDNS 같은 무료 서브도메인으로 충분.
그다음 VM에서 `sudo bash setup.sh` 한 번. 막히면 로그 들고 오면 바로 잡음.

**공개해도 남는 한계 (알고 결정할 것)**
- **학생 로그인에 비밀번호가 없음**(학번+이름). 명단 제한을 켜도 같은 반 학생끼리
  사칭 가능. 교내 LAN에서는 사실상 문제가 아니었지만 인터넷에서는 다름.
  **성적에 직접 반영한다면 학생별 비밀번호가 별도 과제로 필요.**
- gm3는 기구를 그리는 데 필요한 `val`이 여전히 클라이언트로 감(정답 `sf`는 제거됨).

---

### 1순위 — 학습 자료 C~F 개선 (계획 수립부터 다시 할 것)

**대상 파일 동일** (위 백업 규칙 그대로 적용).
- 진행상황 저장 없음(`localStorage` 0회). `showComplete()`가 컨테이너를 `innerHTML`로
  교체해 복습 불가. **이번에 실제로 밟아 확인**: 마지막 섹션에서 `다음`을 한 번 더 누르면
  `showComplete()`가 돌면서 9개 섹션 DOM이 통째로 사라지고, 새로고침 말고는 되돌릴 방법이 없음.
  C 작업할 때 "완료 화면 → 섹션 복귀" 경로를 같이 넣는 게 좋음.
- 점수 집계가 섹션 2·6만. `renderQuiz()`(4·5)·`renderToolQuiz()`(7)는 채점만 하고 점수 없음.
- 문제 고정·순서 고정 → 셔플 + 문항 보강.
- 구글 폰트 CDN 3종 의존 → 오프라인에서 디자인 붕괴. (임베딩 vs 안전한 폰트 스택 대체를
  파일 크기 대비 검토. 현재 63KB.)

**제약**: 단일 HTML 유지, 오프라인 동작, 한국어 "~해요" 체, 9섹션 구조 불변, 접근성은 범위 밖,
따뜻한 종이색 팔레트(`--bg: #FBF6E9`)·손글씨 톤 유지. 모바일 대응은 이미 양호하니 건드리지 말 것.
반올림 섹션의 짝수 맞춤(banker's rounding)은 검산 정확하니 유지.

### 2순위 — 게임 추가 개선 (원할 때)
- **3단계 힌트**: 새 main에는 이미 클라이언트 하드코딩 1단계 힌트(모드별 규칙 한 줄,
  문제당 1회)가 있음. **정답을 흘리지 않음.** → "버그 수정"이 아니라 "2·3단계 추가"라
  성격이 약해졌고 우선순위 낮음. (`room.html`의 `showHint()`)
- **오답 복습 방**("실수 챌린지"): 학생이 자기 오답만 모아 다시 풀기. **선행 작업 필요** —
  현재 `wrongHistory`는 게임 종료 시 사라지므로, 학생 프로필에 `wrongBank`로 영속 저장하는
  작업이 먼저. 가장 큰 작업이라 뒤로.

### 배포/정리 (발주자 결정 필요)
- `feat/learning-ux-v2`를 원격 `main`에 머지하고 학교 노트북에서 `git pull`로 반영할지.
- **머지 전 반드시**: 학교 노트북에서 서버 실행 중이면 정상 종료(Ctrl+C) 후 pull.

---

## 이번에 밝혀진 중요한 사실 (지난 인계서의 오판 정정)

이전 세션에서 낡은 base(`5a2489a`, 4월) 위에서 작업하며 세운 우려들이, 최신 `origin/main`
(`6beb3cd`, 5월)을 실제로 확인하니 대부분 이미 해결돼 있었음:

| 지난 우려 | 실제 (최신 main 기준) |
|---|---|
| 저장이 동기 방식이라 느림 → 디바운스 필요 | **이미 있음**: `atomicWrite` + `makeDebouncedSaver(600ms)` |
| 종료 시 저장 flush 필요 | **이미 있음**: Ctrl+C 시 진행 게임 finalize 후 저장 |
| 힌트가 정답을 그대로 노출 | **아님**: 규칙 한 줄 힌트, 문제당 1회 제한 |
| Render 배포 전 SIGTERM 실측 필요 | **불필요**: Render 포기, 학교 노트북 로컬 운영 |
| 폴링 부하가 무료 호스팅에서 위험 | 무료 호스팅 아님, 학교 와이파이+노트북 1대 → 우선순위 낮음 |

→ 그래서 이번엔 **테마·오답 재열람·명단 등록** 3개만 새 base에 다시 얹었고, 나머지는
불필요하거나 축소됨. 이 판단의 근거는 위 표.

---

## 남은 참고 정보

**범위 밖 발견 (미수정, 기록만)**
- 🔴 **측정값 읽기 모드(gm3)는 정답이 클라이언트에 노출됨.** `viewQuestion()`의 gm3 분기가
  `hide` 여부와 무관하게 `meas` 전체(정답 포함)를 전송. 캔버스로 기구를 그리려면 필요한
  기존 설계지만, 개발자도구로 답을 볼 수 있음. **평가에 쓴다면 별도 과제.**
- `teacher.html`에 `loadAutoApprove()`/`toggleAutoApprove()` 등 죽은 코드 잔존
  (대상 마크업이 5월 단순화 때 삭제됨). 호출부는 이미 주석 처리.
- `/api/teacher/students`가 학생 레코드를 `...r`로 통째 반환(blocked 등 포함). 교사 전용이라
  당장 문제는 없으나 형태가 필터 안 됨.

**주의 지점 (구현 시)**
- npm 의존성 추가 금지. `viewQuestion(q, hide)`의 hide 분기에 새 필드 추가 금지.
- 시즌(`seasonId`)·레이트리밋·`persistedTokens`·비속어 필터(`hasBadWord`) 로직 건드리지 말 것.
- 검증 서버는 반드시 별도 `DATA_DIR`로: `DATA_DIR=<스크래치> PORT=8251 TEACHER_PASSWORD=3000 node server.js`
- 새 테스트는 `test/` 폴더 + `node --test` 방식, `package.json`의 test 스크립트에 파일 추가.
- 명단 로그인 게이팅은 **레이트리밋 뒤**에 둘 것(앞에 두면 명단을 무제한 탐색 가능).

**버려진 것**
- 구 브랜치 `feat/learning-ux` (낡은 base 기반) — 원격에 참조용으로 남김. **쓰지 말 것.**
  거기 딸린 stash(`abandoned: ④ 자동 난이도 WIP`)도 폐기 대상.
- 로컬 `main`의 `5195af1`(학생 성장 시스템 1834줄) — 새 main에는 대신 시즌제가 있어
  **채택 안 함**(발주자 결정: 시즌제 사용).

**환경 메모**
- 검증 중 뜬 정체불명 node 프로세스(PID 36236·34896, 이전 세션 에이전트 잔재)는 이번에
  종료함. 새 세션 시작 시 `node server.js` 유령 프로세스가 있으면 확인 후 정리.
- 2026-09-04 확인: 돌아다니는 node 프로세스 중 `test/preview-server.js` 2개(PID 34436·32924)는
  **이 프로젝트 것이 아니라** `metaverse-gallery` / `reading-assessment` 소속이라 건드리지 않음.
  이 프로젝트 것만 정리하려면 커맨드라인을 꼭 확인할 것
  (`wmic process where "name='node.exe'" get ProcessId,CommandLine`).
- **학습 자료(단일 HTML) 브라우저 검증 팁**: 프로젝트 폴더 밖 로컬 파일은 미리보기 창이
  정적 스냅샷으로 띄워서 스크롤 캡처가 부분만 나오고, **같은 URL로 다시 navigate 해도
  리로드가 안 됨**(상태가 그대로 남음). 실제 검증은 `javascript_tool`로 DOM 값을 직접 읽는 게
  확실하고, 상태를 초기화하려면 **파일명을 바꿔 복사**해서 새 URL로 열어야 함.
